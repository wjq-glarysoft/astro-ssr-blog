# wrangler.toml
name = "astro-ssr-blog"  # 你的 Worker 名称
compatibility_date = "2026-06-30"  # 使用当前日期

# 指定 Worker 入口文件（由 Astro 构建生成）
main = "dist/_worker.js"

# 指定静态资源目录
[assets]
directory = "./dist"

# 环境变量（可选）
[vars]
ENVIRONMENT = "production"